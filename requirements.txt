# Core dependencies for CoderingsTool
# Generated for macOS (Apple Silicon compatible)

ipykernel 

# Web UI
streamlit>=1.40.0
watchdog>=4.0.0

# Data handling
pandas>=2.2.0
numpy>=1.26.0,<2.0.0
openpyxl>=3.1.0
pyreadstat>=1.2.0

# Machine Learning / NLP
scikit-learn>=1.5.0
hdbscan>=0.8.30
umap-learn>=0.5.0
pacmap>=0.7.0               # Alternative to UMAP for dimensionality reduction
zadu>=0.2.0                 # DR quality metrics (trustworthiness, continuity, etc.)
scipy>=1.13.0
kneed>=0.8.0            # Knee/elbow detection for clustering strategy

# Visualization
matplotlib>=3.8.0
plotly>=5.18.0              # For Optuna visualizations
kaleido>=0.2.1              # For saving plotly figures as images
wordcloud>=1.9.0            # Word cloud generation for export visualizations
networkx>=3.0               # Network graph building for export visualizations

# Terminal output
tqdm>=4.60.0                # Progress bars (works natively in Jupyter)

# Hyperparameter optimization
optuna>=3.5.0

# LLM / AI
openai>=1.60.0          # Requires 1.60+ for Responses API
instructor>=1.10.0      # Requires 1.10+ for Mode.RESPONSES_TOOLS
tiktoken>=0.8.0
tenacity>=9.0.0
pydantic>=2.10.0

# Azure OpenAI (for ARM dynamic limits)
azure-identity>=1.15.0
azure-mgmt-cognitiveservices>=13.5.0

# Async utilities
aiolimiter>=1.1.0
nest-asyncio>=1.6.0

# spaCy (optional - for advanced NLP)
spacy>=3.7.0
# Note: Download language models separately:
#   python -m spacy download en_core_web_lg
#   python -m spacy download nl_core_news_lg

# HTTP / API
httpx>=0.28.0
requests>=2.32.0

# Google AI (if using Gemini)
google-genai>=1.0.0

# LangChain (if using)
langchain-core>=0.3.0
langchain-openai>=0.2.0
